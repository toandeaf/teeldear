import { FC } from 'react'
import Card from 'components/Card/Card.tsx'
import './Filters.css'

const Filters: FC = () => {
  return <Card className={'filterBar'}>Filter bar</Card>
}

export default Filters
