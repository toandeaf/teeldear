import { FC } from 'react'
import Card from 'components/Card/Card'
import './Categories.css'

const Categories: FC = () => {
  return <Card className={'categories'}>Categories</Card>
}

export default Categories
